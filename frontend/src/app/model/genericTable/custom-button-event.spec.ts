import { CustomButtonEvent } from './custom-button-event';

describe('CustomButtonEvent', () => {
  it('should create an instance', () => {
    expect(new CustomButtonEvent()).toBeTruthy();
  });
});

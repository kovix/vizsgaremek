import { FindExaminationNamePipe } from './find-examination-name.pipe';

describe('FindExaminationNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FindExaminationNamePipe();
    expect(pipe).toBeTruthy();
  });
});

import { ElapsedPipePipe } from './elapsed-pipe.pipe';

describe('ElapsedPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ElapsedPipePipe();
    expect(pipe).toBeTruthy();
  });
});

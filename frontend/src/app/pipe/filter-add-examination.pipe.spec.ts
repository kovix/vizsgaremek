import { FilterAddExaminationPipe } from './filter-add-examination.pipe';

describe('FilterAddExaminationPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAddExaminationPipe();
    expect(pipe).toBeTruthy();
  });
});

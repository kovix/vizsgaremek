import { DateToHHMMPipe } from './date-to-hhmm.pipe';

describe('DateToHHMMPipe', () => {
  it('create an instance', () => {
    const pipe = new DateToHHMMPipe();
    expect(pipe).toBeTruthy();
  });
});
